# Lumen — Design System

> **Source of truth for AI coding tools.** Generated by StyleSnap on
> 2026-07-04 from 2 captures: `lumen.app` (browser extension) + `Lumen Design
> v3` (Figma). 40 raw tokens → 24 reviewed tokens (9 merges). Schema v2.0.
>
> ⚠️ This is a hand-written **reference example** (`docs/examples/`) — the
> oracle for what the StyleSnap webtool's `design.md` export must produce from
> `docs/fixtures/capture-browser-messy.json` + `capture-figma-clean.json`.

## Rules for the coding agent

- **Use only the tokens below.** Never hardcode a color, size, spacing,
  radius, or shadow that isn't listed.
- **Reference tokens by role first** (`color/action/primary`), primitives only
  when no role fits.
- **If a value you need is missing, check §Gaps** — it's listed there because
  the team hasn't decided it. Propose a value and flag it (`// NEW TOKEN —
  needs sign-off`); don't invent silently.

## Color

Semantic roles first; each points at a primitive. Provenance notes where the
value was captured.

| Role | Primitive | Value | Provenance |
|---|---|---|---|
| `color/action/primary` | `color/brand-blue` | `#2E6BFF` | Figma `Button/Primary` (`color/action/primary`) + `.btn-primary` on lumen.app, 34× |
| `color/action/primary-hover` | `color/brand-blue-deep` | `#2456CC` | `.btn-primary:hover`, 2× |
| `color/border/default` | `color/gray-200` | `#E4E7EC` | `input.field` border, 9× |
| `color/feedback/error` | `color/red-600` | `#D92D20` | `[role=alert]`, 2× |
| `color/surface/card` | `color/white` | `#FFFFFF` | `.card` background, 25× |
| `color/surface/overlay` | `color/ink` @ 50% | `#101828` / 0.5 | Figma `Modal/Scrim` |
| `color/surface/page` | `color/gray-50` | `#F9FAFB` | `body` + Figma `Page/Background`, 10× |
| `color/text/muted` | `color/gray-500` | `#667085` | `.caption`, 12× |
| `color/text/primary` | `color/ink` | `#101828` | body copy + headings + Figma `Text/Body`, 70× |

**Primitives**

| Primitive | Value | Notes |
|---|---|---|
| `color/brand-blue` | `#2E6BFF` | survivor of a 4-way merge (`#2E6BFF` · `#2E6CFF` · `#2F6BFE` · `#3067FF`) |
| `color/brand-blue-deep` | `#2456CC` | hover shade |
| `color/brand-violet` | `#6941C6` | hero gradient stop only |
| `color/gray-200` | `#E4E7EC` | |
| `color/gray-50` | `#F9FAFB` | |
| `color/gray-500` | `#667085` | |
| `color/ink` | `#101828` | |
| `color/red-600` | `#D92D20` | |
| `color/white` | `#FFFFFF` | |

**Gradient**

| Token | Value | Provenance |
|---|---|---|
| `gradient/hero` | linear 135° · `color/brand-blue` 0% → `color/brand-violet` 100% | `section.hero`, 1× |

## Typography

Font: **Inter** (stack: `Inter, system-ui, sans-serif`) — the only captured family.

| Role | Size / Weight / Line-height | Extras | Provenance |
|---|---|---|---|
| `type/display` | 48px / 700 / 1.1 | — | `h1.hero-title` |
| `type/heading` | 30px / 600 / 1.2 | — | Figma `Heading/H2` (`type/heading`) |
| `type/body` | 16px / 400 / 1.5 | — | body copy + Figma `Text/Body`, 48× · a 1.45 line-height variant (3×) was merged into 1.5 |
| `type/caption` | 14px / 500 / 1.4 | — | `.caption`, 8× |
| *(primitive)* `type/label-uppercase` | 14px / 500 / 1.4 | tracking 0.5px, uppercase | `.eyebrow-label`, 4× — kept separate from `type/caption` (tracked uppercase ≠ caption) |

## Foundations

**Spacing scale** — `space/xs` 4 · `space/sm` 8 · `space/md` 16 · `space/lg`
24 · `space/xl` 32 · `space/2xl` 64. (A 15px capture, 2×, was merged into
`space/md` 16. A 12px value, 9×, is captured but **unassigned** — see §Gaps.)

**Radius** — `radius/sm` 8 (`.btn-primary`, 16×) · `radius/md` 12 (`.card` +
Figma `radius/md`, 16×).

**Border width** — `border-width/default` 1 (`input.field`, 12×).

**Shadows**

| Token | Value | Provenance |
|---|---|---|
| `shadow/sm` | 0 1px 2px 0 · `color/ink` @ 5% | `.card`, 10× |
| `shadow/md` | 0 4px 8px −2px · `color/ink` @ 10% | dropdown + Figma `shadow/md`, 10× |

## Gaps — undefined in this system

The consumer must not guess these; they were not captured and not yet decided:

- `color/text/link` — links likely use `color/brand-blue`, unconfirmed.
- `color/action/primary-active` (pressed state), `color/action/secondary`.
- `color/border/focus` — no focus ring captured. Accessibility risk; decide before build.
- `color/feedback/success`, `color/feedback/warning`, `color/feedback/info`.
- `type/mono` — no monospace captured.
- 12px spacing (9×) captured but unassigned to the scale.
- Breakpoints, motion/easing, z-index — never capturable; define manually.

---

*Ordering is deterministic (type → role → name → value) — re-exports diff
cleanly. Generated by StyleSnap · project "Lumen" (derived from Figma file
name, user-editable).*
