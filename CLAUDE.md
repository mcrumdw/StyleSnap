# StyleSnap Web App

All project instructions live in **`AGENTS.md`** (single source of truth for
every coding agent — Claude Code, Cursor, etc.). Read it in full before doing
anything, and treat its rules as hard constraints.

Do not add instructions here — edit `AGENTS.md` instead, so all tools stay in
sync.
